//src/util/config.js

export const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo purposes */
    host: "localhost",
    user: "root",
    password: "760410",
    database: "dbsekolah",
    connectTimeout: 60000
  }
}